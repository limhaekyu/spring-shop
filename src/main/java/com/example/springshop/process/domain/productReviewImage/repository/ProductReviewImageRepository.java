package com.example.springshop.process.domain.productReviewImage.repository;

import com.example.springshop.process.domain.productReviewImage.domain.ProductReviewImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductReviewImageRepository extends JpaRepository<ProductReviewImage, Long> {
}
